# Tasks
- [x] Task 1: 修改 `InspectionConfig.vue` 的表格视图，将“检验规则配置明细”列的内联文本展示替换为“查看明细”按钮。对于没有规则的行，显示“暂无规则配置”。
- [x] Task 2: 在 `InspectionConfig.vue` 中新增一个明细弹窗组件（`el-dialog`），并在其中添加一个数据表格（`el-table`），用于展示规则的检验项目、标准值、公差、单位。
- [x] Task 3: 增加对应的响应逻辑（如 `viewRulesDetail(row, process)`），在点击按钮时，将对应行和工序的规则数据传递给弹窗，并控制弹窗的显示。
